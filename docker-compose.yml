services:
  mongo:
    image: mongo:7
    container_name: izibuy-mongo
    command: ["mongod", "--replSet", "rs0", "--bind_ip_all"]
    ports:
      - "27017:27017"
    volumes:
      - izibuy_mongo_data:/data/db
    restart: unless-stopped

volumes:
  izibuy_mongo_data:
