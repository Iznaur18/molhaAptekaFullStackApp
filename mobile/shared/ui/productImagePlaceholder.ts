/**
 * Нейтральный серый blurhash (плоский ~#9a9a9a, 1×1) — размытое превью,
 * которое expo-image показывает мгновенно, пока грузится реальное фото.
 * Убирает «пустой кадр» и Layout Shift: контейнер уже залит нейтральным
 * тоном, одинаково спокойным в светлой и тёмной теме.
 *
 * Когда бэкенд начнёт отдавать per-image blurhash/thumbhash — прокидывайте
 * его в CachedProductImage через проп `blurhash`, эта константа остаётся
 * фолбэком.
 */
export const PRODUCT_IMAGE_BLURHASH = "00Hx$$";
