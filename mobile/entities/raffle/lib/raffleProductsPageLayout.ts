/** Паритет client `RaffleProductsPage.css`. */
export const RAFFLE_PRODUCTS_PAGE_LAYOUT = {
  mediaHeight: 300,
  mediaHeightWide: 342,
  mediaRadius: 22,
  headerRadius: 20,
  progressRadius: 18,
  statRadius: 14,
  wideBreakpoint: 900,
  swipeDotSize: 8,
  swipeDotsGap: 6,
  swipeDotsBottom: 12,
} as const;
