/** Паритет `client/.../ProductCardBadges.css` + catalog mobile overrides. */
export const PRODUCT_CARD_RATING_LAYOUT = {
  fontSize: 13.12,
  lineHeight: 17.71,
  fontWeight: "600" as const,
  placeholderFontWeight: "500" as const,
  rowGap: 4.48,
  countGap: 2.88,
  iconSize: 14,
  catalogGridFontSize: 12,
  catalogGridLineHeight: 20,
  minHeight: 20,
} as const;
