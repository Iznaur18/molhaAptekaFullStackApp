/** Web `.product-price-offer__btn` + `.app-btn--contrast` (ProductPriceOffer.css, AppButton.css). */
export const PRODUCT_PRICE_OFFER_BUTTON_LAYOUT = {
  minHeight: 44,
  paddingVertical: 12,
  paddingHorizontal: 20,
  borderRadius: 10,
  fontSize: 15,
  fontWeight: "600" as const,
} as const;
