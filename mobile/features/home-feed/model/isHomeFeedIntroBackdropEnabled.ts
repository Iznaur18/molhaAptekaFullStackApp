/**
 * Hero intro-видео за шторкой главной + свайп к нему / long-press replay.
 * `false` — временно: лента сразу, без фона-ролика при загрузке.
 * Вернуть: поставить `true`.
 */
export const IS_HOME_FEED_INTRO_BACKDROP_ENABLED = false;
