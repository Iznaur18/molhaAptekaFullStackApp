/**
 * Контроллеры на `createAsyncRouter`: не ловить ошибки вручную.
 * Бросайте `AppError` из services — `asyncHandler` → `errorHandler` в createApp.
 *
 * @see server/middlewares/errorHandlerMW.js
 * @see server/utils/createAsyncRouter.js
 */

export {};
