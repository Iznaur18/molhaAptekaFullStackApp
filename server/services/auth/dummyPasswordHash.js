/**
 * Фиктивный bcrypt-хеш (cost=10, как в registerUserController) для сравнения
 * с постоянным временем на логине.
 *
 * Зачем: если для несуществующего email отвечать сразу, без bcrypt.compare,
 * появляется тайминговый оракул — по времени ответа можно отличить
 * «есть такой email» от «нет». Мы всегда прогоняем bcrypt.compare: для
 * реального юзера — против его хеша, для отсутствующего — против этого dummy.
 *
 * Это не секрет: хеш выброшенной случайной строки, аккаунта с таким паролем
 * не существует. Cost должен совпадать с боевым (bcrypt.genSalt(10)), иначе
 * время сравнения будет отличаться и оракул вернётся.
 */
export const DUMMY_PASSWORD_HASH =
  "$2b$10$kxzSbN9DNhzyfE.a.vmJPuqiVaeJm6OW163XrEhSR9qh0lx9jR3GS";
