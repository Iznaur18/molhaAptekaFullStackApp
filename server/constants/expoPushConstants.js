/** Макс. устройств на пользователя (старые токены вытесняются). */
export const EXPO_PUSH_TOKENS_MAX_PER_USER = 5;

/** Префиксы валидного Expo push token. */
export const EXPO_PUSH_TOKEN_PREFIXES = ["ExponentPushToken[", "ExpoPushToken["];
