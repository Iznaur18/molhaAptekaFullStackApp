diff --git a/node_modules/react-native-gesture-handler/lib/commonjs/web/utils.js b/node_modules/react-native-gesture-handler/lib/commonjs/web/utils.js
index 38a68f3..f3dfb0d 100644
--- a/node_modules/react-native-gesture-handler/lib/commonjs/web/utils.js
+++ b/node_modules/react-native-gesture-handler/lib/commonjs/web/utils.js
@@ -224,10 +224,15 @@ function isRNSVGElement(viewRef) {
 //
 // Second condition was introduced to handle case where SVG element was wrapped with
 // `createAnimatedComponent` from Reanimated.
+// React 19 moved `ref` onto `props` and logs a deprecation warning when
+// `element.ref` is accessed directly. Only read `element.ref` on React <= 18,
+// where the ref is not present in `props`.
+const CAN_READ_ELEMENT_REF = parseInt(require("react").version, 10) < 19;
 function isRNSVGNode(node) {
   // If `ref` has `rngh` field, it means that component comes from Gesture Handler. This is a special case for
   // `Text` component, which is present in `RNSVGElements` set, yet we don't want to treat it as SVG.
-  if (node.ref?.rngh) {
+  const nodeRef = node?.props?.ref ?? (CAN_READ_ELEMENT_REF ? node?.ref : undefined);
+  if (nodeRef?.rngh) {
     return false;
   }
   return Object.getPrototypeOf(node?.type)?.name === 'WebShape' || RNSVGElements.has(node?.type?.displayName);
diff --git a/node_modules/react-native-gesture-handler/lib/module/web/utils.js b/node_modules/react-native-gesture-handler/lib/module/web/utils.js
index e9ae250..a08c84a 100644
--- a/node_modules/react-native-gesture-handler/lib/module/web/utils.js
+++ b/node_modules/react-native-gesture-handler/lib/module/web/utils.js
@@ -212,10 +212,15 @@ export function isRNSVGElement(viewRef) {
 //
 // Second condition was introduced to handle case where SVG element was wrapped with
 // `createAnimatedComponent` from Reanimated.
+// React 19 moved `ref` onto `props` and logs a deprecation warning when
+// `element.ref` is accessed directly. Only read `element.ref` on React <= 18,
+// where the ref is not present in `props`.
+const CAN_READ_ELEMENT_REF = parseInt(require('react').version, 10) < 19;
 export function isRNSVGNode(node) {
   // If `ref` has `rngh` field, it means that component comes from Gesture Handler. This is a special case for
   // `Text` component, which is present in `RNSVGElements` set, yet we don't want to treat it as SVG.
-  if (node.ref?.rngh) {
+  const nodeRef = node?.props?.ref ?? (CAN_READ_ELEMENT_REF ? node?.ref : undefined);
+  if (nodeRef?.rngh) {
     return false;
   }
   return Object.getPrototypeOf(node?.type)?.name === 'WebShape' || RNSVGElements.has(node?.type?.displayName);
diff --git a/node_modules/react-native-gesture-handler/src/web/utils.ts b/node_modules/react-native-gesture-handler/src/web/utils.ts
index 2825eff..2adf0cd 100644
--- a/node_modules/react-native-gesture-handler/src/web/utils.ts
+++ b/node_modules/react-native-gesture-handler/src/web/utils.ts
@@ -279,10 +279,18 @@ export function isRNSVGElement(viewRef: SVGRef | GestureHandlerRef) {
 //
 // Second condition was introduced to handle case where SVG element was wrapped with
 // `createAnimatedComponent` from Reanimated.
+// React 19 moved `ref` onto `props` and logs a deprecation warning when
+// `element.ref` is accessed directly. Only read `element.ref` on React <= 18,
+// where the ref is not present in `props`.
+const CAN_READ_ELEMENT_REF =
+  parseInt(require('react').version, 10) < 19;
+
 export function isRNSVGNode(node: any) {
   // If `ref` has `rngh` field, it means that component comes from Gesture Handler. This is a special case for
   // `Text` component, which is present in `RNSVGElements` set, yet we don't want to treat it as SVG.
-  if (node.ref?.rngh) {
+  const nodeRef =
+    node?.props?.ref ?? (CAN_READ_ELEMENT_REF ? node?.ref : undefined);
+  if (nodeRef?.rngh) {
     return false;
   }
 
