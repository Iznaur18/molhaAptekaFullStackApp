import { UserDetailsPage } from "../../pages/user-details/ui/UserDetailsPage.jsx";

/** `/user/:userId` — чужой профиль (полноэкран, как в приложении). */
export function UserRoutePage() {
  return <UserDetailsPage />;
}
