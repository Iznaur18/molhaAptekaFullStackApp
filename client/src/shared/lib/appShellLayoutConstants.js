/** Совпадает с `--app-shell-max-width` в `index.css` (desktop content cap). */
export const WEB_APP_SHELL_MAX_WIDTH_PX = 1200;

/** Имя CSS container для layout-запросов — см. `index.css`. */
export const APP_VIEWPORT_CONTAINER_NAME = "app-viewport";

/** Tablet upper bound: `@container … (max-width: 1023px)`. */
export const APP_SHELL_TABLET_MAX_PX = 1023;

/** Desktop from this width (inclusive). */
export const APP_SHELL_DESKTOP_MIN_PX = 1024;
