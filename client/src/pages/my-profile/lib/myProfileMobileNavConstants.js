/** Matches `@container app-viewport (max-width: 900px)` in MyProfilePage.css */
export const MY_PROFILE_DRAWER_LAYOUT_MAX_PX = 900;

/** Sidebar transform 0.28s — unmount after exit finishes */
export const MY_PROFILE_MOBILE_NAV_EXIT_MS = 280;
