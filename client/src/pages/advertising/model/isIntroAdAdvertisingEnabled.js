/**
 * Карточка «Intro-ролик» на странице Реклама.
 * `false` — временно: серый disabled, без оформления заявок.
 * Вернуть: поставить `true`.
 */
export const IS_INTRO_AD_ADVERTISING_ENABLED = false;
