import { createContext } from "react";

/**
 * Как грузить текущее фото карточки: лениво (по умолчанию) или сразу.
 *
 * Лента блоками выставляет "eager" глубже первых экранов: блок монтируется
 * заранее, за экран до показа, и фото успевает загрузиться и декодироваться
 * раньше, чем карточка въедет в экран.
 */
export const ProductCardImageLoadingContext = createContext(
  /** @type {"lazy" | "eager"} */ ("lazy"),
);
